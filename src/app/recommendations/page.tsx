import RecommendationPage from "@/components/recommendations/RecommendationPage";

export const metadata = {
  title: "Your Room Recommendations — Dwelliq",
  description: "Three options for every piece. Best within budget, best if flexible, best local.",
};

export default function RecommendationsPage() {
  return <RecommendationPage />;
}
